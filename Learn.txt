Javascript

variables {
 let
 const
 var
}

dataType {
'' - string
20 - number
true - boolean
undefined - undefined
}

js function

push() - push data into the last
unshift - push data into trhe first
pop - remove 
indexOf - get the index of data
typeOf - checking of dataTypes

xhr.setRequestHeader('Content-type', 'application/x-www-form-urlencoded');